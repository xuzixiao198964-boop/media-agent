# 小说视频生成系统 — 追加需求说明书

## 文档信息
- **文档编号**: FPRS-NOVEL-001
- **文档标题**: 小说分章视频生成系统需求说明书
- **项目名称**: Media Agent — 小说故事影片生成扩展
- **创建日期**: 2026-03-29
- **创建人**: Media Agent Assistant
- **状态**: 📋 初稿
- **版本**: 1.1
- **前置文档**: `Media-Agent-最终版需求说明书.md`（基础短视频系统）

---

## 1. 项目背景与目标

### 1.1 背景

现有 Media Agent 系统已实现「资讯 → 短视频 → 发布」的完整链路，支持资讯口播、文案生成、TTS 配音、VideoRetalk 口型同步等能力。

本需求在此基础上扩展**小说故事影片生成**能力：将 `ai-novel-agent` 系统发布的长篇小说按章节拆解，生成高质量的连续剧式故事影片，要求跨章节声音一致、画面连贯、支持人工审核与返工。

### 1.2 目标

1. 从 `ai-novel-agent` 的已发布小说中选取作品，按章节生成故事影片
2. 每章视频时长 3-10 分钟，具有故事性和观赏性
3. 多角色配音，不同角色使用不同声线，跨章节一致
4. AI 生成场景画面（图片 + 动态视频），视觉风格统一
5. **双重审核机制**：脚本审核 + 成片审核，不通过可驳回重做
6. 支持增量重做（只重做有问题的部分，不浪费已通过内容）

### 1.3 与现有系统的关系

本需求为**追加扩展**，不替换现有短视频生成能力。现有的资讯口播、文案视频、TTS、FFmpeg 等基础能力继续保留并复用。

---

## 2. 系统架构

### 2.1 整体流程

```
①导入小说（从 ai-novel-agent 拉取）
    ↓
②AI 拆章 → 角色识别 → 结构化分镜脚本生成（含 visual_prompt）
    ↓
③【脚本审核】── 不通过 → AI 根据审核意见重写 → 回到③
    ↓ 通过
④【图片提示词评审】── AI 评审 visual_prompt 是否满足小说场景需求
    ├── 不通过 → AI 重新生成提示词 → 回到④（最多3轮）
    ↓ 通过
⑤AI 场景图片生成（硅基流动/PIL 占位）
    ↓
⑥【图片评审】── AI 评审生成的图片是否满足提示词要求、是否错位
    ├── 不通过 → 重新生成图片（可更新提示词后重新生成）→ 回到⑥（最多3轮）
    ↓ 通过
⑦多角色配音 + BGM 匹配
    ↓
⑧AI 图生视频（图片评审通过后才开始）
    ↓
⑨FFmpeg 合成章节成片
    ↓
⑩【成片审核】── 不通过 → 定位问题项 → 增量重做 → 回到⑩
    ↓ 通过
⑪入库 / 发布
```

> **重要变更（v1.1）**：图片评审（步骤④⑥）必须全部通过后，才能开始视频创作（步骤⑧）。这确保了画面质量可控，避免浪费视频生成成本。

### 2.2 外部系统集成

| 系统 | 地址 | 用途 |
|------|------|------|
| ai-novel-agent | `http://104.244.90.202:9000` | 小说内容来源 |
| media-agent（本系统） | `http://104.244.90.202:9090` | 视频生成与管理 |

### 2.3 调用 ai-novel-agent 的 API

| 接口 | 方法 | 用途 |
|------|------|------|
| `/novel-api/novels` | GET | 获取已发布小说列表（支持分类、排序、分页） |
| `/novel-api/novels/by-slug/{slug}` | GET | 小说详情 + 章节目录（无正文） |
| `/novel-api/novels/by-slug/{slug}/chapter/{chapter_no}` | GET | 获取单章全文（chapter_no 从 1 开始） |
| `/novel-api/categories` | GET | 获取分类列表 |
| `/novel-api/search?q=关键词` | GET | 搜索小说 |

---

## 3. 数据模型

### 3.1 小说项目（NovelProject）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| user_id | int | FK → users |
| novel_slug | str | ai-novel-agent 中的小说 slug |
| novel_title | str | 书名（冗余存储） |
| novel_author | str | 作者 |
| novel_intro | text | 简介 |
| cover_url | str | 封面图 URL |
| narrator_voice_id | str | 旁白声线 ID（项目级锁定） |
| visual_style | text | 视觉风格描述（如"中国工笔画风格，暖色调"） |
| default_bgm_id | int | FK → bgm_library，默认背景音乐 |
| tts_language | str | 语言，默认 zh-CN |
| status | str | active / archived |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 3.2 角色表（NovelCharacter）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| project_id | int | FK → novel_projects |
| name | str | 角色名（如"林黛玉"） |
| gender | str | male / female / neutral |
| voice_id | str | Fish Audio 声线 ID（项目级锁定） |
| voice_sample_url | str | 参考音频路径（用于声音克隆） |
| personality | text | 性格描述（辅助 AI 生成脚本时控制语气） |
| appearance | text | 外貌描述（注入图片生成 prompt 保持一致性） |
| created_at | datetime | 创建时间 |

### 3.3 章节任务（NovelChapter）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| project_id | int | FK → novel_projects |
| chapter_no | int | 章节号（对应 ai-novel-agent 的 chapter_no） |
| title | str | 章节标题 |
| raw_text | text | 原文（从 ai-novel-agent 拉取缓存） |
| script | JSON | 结构化分镜脚本 |
| script_status | str | draft / reviewing / approved / rejected |
| script_review_notes | text | 脚本审核意见 |
| video_status | str | pending / generating / compositing / reviewing / approved / rejected / published |
| video_review_notes | text | 成片审核意见 |
| generation_job_id | int | FK → generation_jobs（最终成片关联） |
| audio_assets | JSON | 各 scene 的音频文件路径 |
| visual_assets | JSON | 各 scene 的图片/视频文件路径 |
| bgm_id | int | FK → bgm_library，本章 BGM（可覆盖项目默认） |
| bgm_volume | float | BGM 音量 0.0-1.0 |
| estimated_duration | int | 预估时长（秒） |
| actual_duration | float | 实际时长（秒） |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 3.4 章节任务补充字段（v1.1 新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| prompt_status | str | pending / reviewing / approved / rejected（提示词评审状态） |
| prompt_review_notes | text | 提示词评审意见 |
| prompt_review_round | int | 提示词评审轮次（默认0） |
| image_status | str | pending / generating / reviewing / approved / rejected（图片评审状态） |
| image_review_notes | text | 图片评审意见 |
| image_review_round | int | 图片评审轮次（默认0） |
| image_prompts | JSON | 各 scene 的最终评审通过的提示词 |

### 3.5 审核记录（ChapterReview）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| chapter_id | int | FK → novel_chapters |
| review_stage | str | script / prompt / image / video（v1.1扩展：增加 prompt 和 image） |
| review_round | int | 第几轮审核（1, 2, 3...） |
| status | str | pending / approved / rejected |
| reviewer_id | int | FK → users（AI 评审时为 null） |
| reviewer_type | str | ai / human（评审者类型，v1.1新增） |
| review_detail | JSON | 评审明细（v1.1新增，存储各scene的评审结果） |
| created_at | datetime | 审核时间 |

### 3.5 审核明细（ReviewItem）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| review_id | int | FK → chapter_reviews |
| target_type | str | scene_visual / scene_audio / bgm / subtitle / overall |
| target_scene_id | int | 对应 scene 序号（可为空，如 BGM 审核） |
| passed | bool | 是否通过 |
| notes | text | 审核意见（如"配音语气太平淡，应该更激昂"） |
| action | str | regenerate / replace_manual / adjust_params / skip |

### 3.6 BGM 库（BgmLibrary）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| user_id | int | FK → users（系统预置的 user_id 为空） |
| name | str | 曲名 |
| mood | str | epic / sad / romantic / tense / peaceful / comedy / mysterious |
| file_path | str | 文件存储路径 |
| duration_sec | float | 时长 |
| source | str | pixabay / mixkit / custom / suno |
| created_at | datetime | 上传时间 |

---

## 4. 分镜脚本格式

### 4.1 AI 生成的结构化脚本

每章原文经 DeepSeek 拆解后，生成如下 JSON 结构：

```json
{
  "chapter_no": 1,
  "title": "第一回 甄士隐梦幻识通灵",
  "summary": "本章引出贾雨村和甄士隐的初遇，为全书埋下伏笔。",
  "estimated_duration_sec": 240,
  "scenes": [
    {
      "scene_id": 1,
      "type": "narration",
      "speaker": "narrator",
      "text": "话说贾雨村在扬州城中寄居，日日以诗酒自遣。",
      "visual_prompt": "古代扬州城街景，黄昏时分，一个落魄书生独自在酒馆临窗饮酒，远处是繁华的街市",
      "mood": "melancholy",
      "duration_hint": 8
    },
    {
      "scene_id": 2,
      "type": "dialogue",
      "speaker": "贾雨村",
      "text": "玉在匮中求善价，钗于奁内待时飞。",
      "visual_prompt": "书生站在窗前仰望明月，衣衫微旧但眼神坚定",
      "mood": "ambitious",
      "duration_hint": 6
    },
    {
      "scene_id": 3,
      "type": "narration",
      "speaker": "narrator",
      "text": "甄士隐在隔壁花园散步，忽然听到此联，不禁击节叹赏。",
      "visual_prompt": "一位中年儒商在精致的私家花园中漫步，月光洒在假山和池塘上",
      "mood": "curious",
      "duration_hint": 7
    },
    {
      "scene_id": 4,
      "type": "dialogue",
      "speaker": "甄士隐",
      "text": "兄何不去求取功名，以展平生之志？",
      "visual_prompt": "两人在花园石桌旁对坐，月下品茶",
      "mood": "warm",
      "duration_hint": 5
    },
    {
      "scene_id": 5,
      "type": "transition",
      "speaker": null,
      "text": null,
      "visual_prompt": "画面淡出，月亮升至中天，时间流逝",
      "mood": "neutral",
      "duration_hint": 2
    }
  ]
}
```

### 4.2 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| scene_id | 是 | 场景序号，从 1 开始 |
| type | 是 | narration（旁白）/ dialogue（对话）/ transition（过渡转场）/ monologue（独白） |
| speaker | 是 | 说话人。narrator = 旁白；角色名须与 NovelCharacter.name 完全一致；transition 为 null |
| text | 否 | 台词/旁白文本。transition 可为 null |
| visual_prompt | 是 | 场景画面描述，用于 AI 图片/视频生成 |
| mood | 是 | 情绪标签，用于 TTS 语气控制和 BGM 匹配 |
| duration_hint | 是 | 建议时长（秒），控制节奏 |

### 4.3 角色一致性约束

- AI 生成脚本时，所有 speaker 必须映射到 `NovelCharacter` 表中已有的角色
- 新出现的说话人，AI 应在脚本中标注，系统提示用户为其分配声线
- 同一角色在所有章节中使用完全相同的 speaker 名称

---

## 5. 审核流程

### 5.1 脚本审核（第一轮）

**触发时机**：AI 完成分镜脚本生成后。

**审核内容**：
1. 旁白与对话的拆分是否合理
2. 角色归属是否正确（谁说了哪句话）
3. 画面描述是否与文意一致
4. 整体节奏和时长是否合适
5. 情绪标注是否恰当

**审核操作**：
- **通过**：整章脚本进入配音+画面生成阶段
- **驳回 + 整体意见**：AI 根据意见重新生成整章脚本
- **逐场景修改**：用户直接编辑某个 scene 的文本或画面描述（不触发 AI 重写）

**驳回重写规则**：
- AI 重写时注入用户的审核意见作为约束
- 保留用户已手动修改过的 scene（标记为 `user_edited`），仅重写未编辑的部分
- 重写后回到 `draft` 状态，需再次审核

### 5.2 图片提示词评审（第二轮）

**触发时机**：脚本审核通过后，在生成图片之前。

**评审方式**：AI 自动评审（调用 DeepSeek），也可由人工介入修改。

**评审内容**：
1. 每个 scene 的 `visual_prompt` 是否准确描述了小说场景
2. 提示词是否包含足够的场景细节（环境、人物、动作、光线等）
3. 提示词是否与小说上下文一致（不偏离剧情）
4. 提示词是否包含了角色外貌特征（`character.appearance`）
5. 提示词之间是否存在矛盾或不连贯

**评审输出**（JSON 结构）：
```json
{
  "overall_pass": false,
  "scenes": [
    {
      "scene_id": 1,
      "pass": true,
      "score": 85,
      "notes": ""
    },
    {
      "scene_id": 3,
      "pass": false,
      "score": 40,
      "notes": "提示词中描述的环境与小说场景不符，原文是夜晚的花园，提示词写成了白天街市",
      "suggested_prompt": "夜晚的私家花园，月光照在假山和池塘上，一位中年儒商独自散步"
    }
  ]
}
```

**评审规则**：
- 每个 scene 的提示词单独评审，score ≥ 70 视为通过
- 任何一个 scene 不通过，则整章提示词评审不通过
- 不通过的 scene，AI 自动生成改进后的 `suggested_prompt`
- 用户可选择接受 AI 建议，或手动修改提示词
- **最多 3 轮**自动重试，超过则需人工介入

**状态流转**：
- `prompt_pending` → `prompt_reviewing` → `prompt_approved` / `prompt_rejected`

### 5.3 图片评审（第三轮）

**触发时机**：图片生成完成后，在视频生成之前。

**评审方式**：AI 自动评审（调用视觉模型如 DeepSeek-VL 或 Gemini），也可由人工介入。

**评审内容**：
1. 生成的图片是否与提示词描述一致
2. 图片中是否存在明显错位（如人物变形、肢体错乱、五官异常）
3. 图片风格是否与项目设定的 `visual_style` 一致
4. 图片中的文字/水印是否需要清除
5. 图片的构图和色调是否适合视频使用

**评审输出**（JSON 结构）：
```json
{
  "overall_pass": false,
  "scenes": [
    {
      "scene_id": 1,
      "pass": true,
      "score": 90,
      "notes": ""
    },
    {
      "scene_id": 2,
      "pass": false,
      "score": 30,
      "issues": ["人物手部变形", "背景与提示词不符"],
      "action": "regenerate"
    }
  ]
}
```

**评审规则**：
- 每个 scene 的图片单独评审，score ≥ 70 视为通过
- 不通过的图片会被标记需要重新生成
- 重新生成时可选择：
  - 使用相同提示词重新生成（随机种子不同）
  - 修改提示词后重新生成
  - 更换负面提示词（针对特定缺陷如 "deformed hands"）
- **最多 3 轮**自动重试，超过则使用 PIL 占位图兜底
- **所有 scene 的图片评审全部通过后，才能开始视频生成**

**状态流转**：
- `image_pending` → `image_reviewing` → `image_approved` / `image_rejected`

### 5.4 成片审核（第四轮）

**触发时机**：章节成片合成完成后。

**审核维度（4 项）**：

| 审核项 | 检查内容 | 不通过时的处理 |
|--------|---------|---------------|
| 画面 | 场景画面是否与脚本描述一致、风格是否统一、有无瑕疵 | 标记 scene_id → 仅重新生成该场景画面 |
| 配音 | 角色声线是否正确、语气是否合适、有无吞字错读 | 标记 scene_id → 仅重新合成该段 TTS |
| BGM | 背景音乐是否与情绪匹配、音量是否合适 | 更换 BGM 或调整音量 → 重新混音 |
| 整体 | 音画同步是否正确、字幕有无错误、节奏是否合理 | 调整时间轴参数 → 重新合成 |

**增量重做规则**：
- 只重做被驳回的部分，通过的素材直接复用
- 替换素材后重新合成（FFmpeg），不需要重走全流程
- 生成新成片后再次进入成片审核

### 5.5 章节状态机

```
raw_text ──→ script_draft ──→ script_reviewing ─┬──→ script_approved
                  ↑                             │         │
                  └──── script_rejected ←───────┘         │
                                                          ↓
                                                prompt_reviewing ──┐
                                                     │             │
                                                prompt_approved  prompt_rejected
                                                     │             │
                                                     ↓         (重新生成提示词, max 3轮)
                                                image_generating
                                                     │
                                                     ↓
                                                image_reviewing ──┐
                                                     │             │
                                                image_approved   image_rejected
                                                     │             │
                                                     ↓         (重新生成图片, max 3轮)
                                                generating_video
                                                     │
                                                     ↓
                                                compositing
                                                     │
                                                     ↓
                                   ┌──→ video_reviewing ←──────┐
                                   │         │                  │
                                   │    ┌────┴────┐             │
                                   │    │         │             │
                                   │ approved  rejected         │
                                   │    │         │             │
                                   │    ↓    partial_regen ──→ recompositing
                                   │ published                  │
                                   └────────────────────────────┘
```

> **注意**：图片评审（prompt_reviewing + image_reviewing）全部通过后才进入 generating_video 阶段。

---

## 6. 多角色配音系统

### 6.1 声线管理

每个小说项目维护独立的角色-声线映射表，确保跨章节一致。

**声线来源**：
- **Fish Audio 预设声线**：从 Fish Audio 声线库选择（男声/女声/老年/少年等）
- **声音克隆**：用户上传参考音频 → 调用 Fish Audio 克隆 API → 生成专属声线 model_id
- **系统预设旁白**：腾讯云 TTS / Edge TTS / OpenAI TTS 作为旁白备选

**声线锁定机制**：
- 项目创建时分配声线，后续所有章节自动继承
- 声线变更需用户确认，且可选择"重新生成已有章节的配音"

### 6.2 配音合成流程

```
approved_script.scenes[]
    │
    ├── scene[type=narration]  → narrator_voice_id  → Fish Audio TTS → audio_01.mp3
    ├── scene[type=dialogue]   → character.voice_id  → Fish Audio TTS → audio_02.mp3
    ├── scene[type=transition] → (静音或环境音)       → FFmpeg 生成    → silence_03.mp3
    └── scene[type=dialogue]   → character.voice_id  → Fish Audio TTS → audio_04.mp3
    │
    ▼ FFmpeg concat + crossfade（场景间 0.3s 渐变）
    chapter_N_voice.mp3
    │
    ▼ 叠加 BGM（根据 mood 自动匹配或用户指定）
    chapter_N_final_audio.mp3
```

### 6.3 Fish Audio API 调用规范

| 参数 | 说明 |
|------|------|
| model_id | 声线 ID（角色表中的 voice_id） |
| text | 台词文本 |
| format | mp3 |
| 情绪控制 | 通过 prompt 或 SSML 控制语气（如"用悲伤的语气说"） |

---

## 7. AI 场景画面生成

### 7.1 两步生成策略

采用「先生图 → 审核 → 再生视频」的两步策略，确保画面质量可控：

```
scene.visual_prompt + project.visual_style + character.appearance
    │
    ▼ 第一步：生成静态场景图
    硅基流动 API（Flux 模型） → scene_N.png
    │
    ▼ 用户可在成片审核时检查图片
    │
    ▼ 第二步：图片动态化
    可灵 AI（图生视频） → scene_N.mp4（5-8秒）
```

### 7.2 视觉一致性保障

| 手段 | 说明 |
|------|------|
| 全局风格前缀 | 每个图片/视频 prompt 前拼接 `project.visual_style`（如"中国工笔画风格，暖色调，电影质感"） |
| 角色外貌注入 | 涉及角色的场景，prompt 中注入 `character.appearance`（如"林黛玉：瓜子脸，柳叶眉，身穿淡绿色衣裙"） |
| 固定负面提示词 | 统一的 negative_prompt 排除常见瑕疵（如"变形、模糊、现代元素"） |

### 7.3 API 调用

#### 硅基流动（图片生成）

| 参数 | 说明 |
|------|------|
| model | Flux.1-schnell 或 SDXL |
| prompt | visual_style + appearance + scene.visual_prompt |
| size | 1920x1080（横屏）或 1080x1920（竖屏） |
| 预估成本 | ￥0.01-0.03/张 |

#### 可灵 AI（视频生成）

| 参数 | 说明 |
|------|------|
| 输入 | 场景图片 + 运镜描述 |
| 时长 | 5 秒 |
| 分辨率 | 1080p |
| 预估成本 | ￥0.3-2/条 |

---

## 8. 背景音乐系统

### 8.1 BGM 库

系统维护一个按情绪分类的 BGM 库，支持：
- **系统预置**：从免版权网站预先下载的 BGM（Pixabay Music、Mixkit、Free Music Archive）
- **用户上传**：用户自行上传 BGM 文件
- **后期扩展**：接入 Suno AI 按场景自动生成 BGM

### 8.2 情绪分类标签

| 标签 | 中文 | 适用场景 |
|------|------|---------|
| epic | 史诗 | 战斗、宏大场面、高潮 |
| sad | 悲伤 | 离别、死亡、回忆 |
| romantic | 浪漫 | 爱情、相遇、温馨 |
| tense | 紧张 | 悬疑、追逐、危机 |
| peaceful | 宁静 | 日常、风景、休憩 |
| comedy | 欢快 | 搞笑、轻松、庆祝 |
| mysterious | 神秘 | 玄幻、梦境、未知 |
| heroic | 英勇 | 觉醒、逆转、英雄时刻 |

### 8.3 BGM 匹配规则

1. AI 生成分镜脚本时，每个 scene 标注 `mood`
2. 系统统计本章各 mood 的占比，选择最匹配的 BGM
3. 用户可在成片审核时手动更换 BGM 或调整音量
4. 同一小说项目可设置默认 BGM，章节级别可覆盖

---

## 9. 成片合成

### 9.1 合成流程

```
各 scene 的视频/图片素材
    +
各 scene 的音频文件
    +
字幕文件（SRT）
    +
BGM
    +
片头模板（可选：书名 + 第 N 章 + 章节标题）
    +
片尾模板（可选：下章预告）
    │
    ▼ FFmpeg 合成
    chapter_N_final.mp4
```

### 9.2 合成参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 输出分辨率 | 1080x1920（竖屏）或 1920x1080（横屏） | 用户可选 |
| 视频编码 | H.264 | 兼容性最好 |
| 音频编码 | AAC 128kbps | 标准质量 |
| 场景间转场 | 1s 淡入淡出 | 可配置 |
| 字幕样式 | 白色描边，底部居中 | 可配置字体、大小、颜色 |
| BGM 音量 | 0.15（配音场景）/ 0.4（纯画面场景） | 自动调节 |
| 片头时长 | 3-5s | 书名+章节信息 |

---

## 10. API 依赖清单

### 10.1 已有 API（直接复用）

| API | 用途 | 配置 |
|-----|------|------|
| DeepSeek | 小说解析、角色识别、分镜脚本生成、审核意见重写 | 已配置 |
| 腾讯云 TTS | 旁白基础配音（备选） | 已配置 |
| Edge TTS | 旁白备选 | 无需配置 |
| FFmpeg | 音频拼接、视频合成、字幕烧录 | 已安装 |
| VideoRetalk | 口型同步（有人脸场景可选用） | 已配置 |

### 10.2 需新增 API

| API | 用途 | 申请地址 | 环境变量 |
|-----|------|---------|---------|
| **Fish Audio** | 多角色 TTS + 声音克隆 | [fish.audio](https://fish.audio) → API Keys | `FISH_AUDIO_API_KEY` |
| **硅基流动** | AI 图片生成（Flux） | [siliconflow.cn](https://siliconflow.cn) → API Keys | `SILICONFLOW_API_KEY` |
| **可灵 AI** | AI 视频生成（图生视频） | [platform.klingai.com](https://platform.klingai.com) | `KLING_ACCESS_KEY`, `KLING_SECRET_KEY` |

### 10.3 可选 API

| API | 用途 | 申请地址 | 环境变量 |
|-----|------|---------|---------|
| Gemini | 长上下文角色分析（100万 token） | [aistudio.google.com](https://aistudio.google.com) | `GEMINI_API_KEY` |
| Suno AI | AI 音乐生成 | [suno.com](https://suno.com) | `SUNO_API_KEY` |

---

## 11. 成本估算

### 11.1 单章成本预估（约 20 个场景，3-5 分钟视频）

| 项目 | 单价 | 数量 | 费用 |
|------|------|------|------|
| DeepSeek（脚本生成） | ￥0.01/千 token | ~5K tokens | ￥0.05 |
| Fish Audio（多角色配音） | ￥0.1/千字符 | ~3000 字 | ￥0.3 |
| 硅基流动（场景图片） | ￥0.02/张 | 20 张 | ￥0.4 |
| 可灵 AI（场景视频） | ￥1/条 | 20 条 | ￥20 |
| **单章合计** | | | **约 ￥21** |

### 11.2 整本小说成本预估

| 规模 | 章节数 | 预估总成本 |
|------|--------|-----------|
| 短篇（10 章） | 10 | ￥210 |
| 中篇（30 章） | 30 | ￥630 |
| 长篇（100 章） | 100 | ￥2100 |

**成本优化策略**：
- 非关键场景只生成图片 + Ken Burns 运镜，不生成视频（节省 80% 画面成本）
- 对话场景用静态画面 + 字幕 + 配音即可（省去视频生成）
- 仅高潮/关键场景使用 AI 视频生成

---

## 12. 非功能性需求

### 12.1 性能

- 单章节脚本生成：< 30 秒
- 单 scene TTS 合成：< 10 秒
- 单 scene 图片生成：< 15 秒
- 单 scene 视频生成：< 120 秒
- 整章合成（FFmpeg）：< 60 秒
- 单章端到端（无审核等待）：< 30 分钟

### 12.2 可靠性

- API 调用失败自动重试 3 次
- 每个 scene 的素材独立生成，单个失败不影响其他
- 所有中间产物（音频、图片、视频）持久化存储，避免重复生成

### 12.3 存储

- 单章素材：约 200-500 MB（音频 + 图片 + 视频片段 + 成片）
- 整本小说（30 章）：约 6-15 GB
- 定期清理策略：成片审核通过后，可选删除中间素材

---

## 13. 开发阶段规划

### 第一阶段：核心链路（MVP）
1. 小说导入（对接 ai-novel-agent API）
2. 项目管理 + 角色管理
3. AI 分镜脚本生成（DeepSeek）
4. 脚本审核流程
5. 多角色配音（Fish Audio）
6. BGM 库 + 自动匹配
7. FFmpeg 成片合成（图片 + 配音 + 字幕 + BGM）
8. 成片审核流程 + 增量重做

### 第二阶段：画面增强
1. AI 图片生成（硅基流动 Flux）
2. AI 视频生成（可灵）
3. 视觉一致性控制
4. 片头片尾模板

### 第三阶段：高级功能
1. Gemini 长上下文角色分析
2. AI 配乐（Suno）
3. 批量章节生成
4. 发布到抖音/小红书

---

## 14. 部署与迁移需求（v1.1 新增）

### 14.1 服务器资源独占

- 服务器 `104.244.90.202` 上的所有资源独属本项目（media-agent）
- 旧的 ai-novel-agent 和 media-agent 服务由新项目替代
- 新项目使用 **80 端口**（HTTP 标准端口），不再使用 9090/9000

### 14.2 数据迁移策略

- **小说数据保留**：服务器上 ai-novel-agent 生成的小说文本数据（`/opt/ai-novel-agent/data/`）保留不删除
- **视频数据全部删除**：服务器上已生成的视频文件（`/data/outputs/`）全部清理释放磁盘空间
- **数据库保留**：PostgreSQL 中的数据表保留，仅做 schema 升级

### 14.3 服务替换

| 旧服务 | 端口 | 处理方式 |
|--------|------|---------|
| ai-novel-agent | 9000 | 停止并禁用 systemd 服务 |
| media-agent（旧） | 9090 | 停止并替换为新版本 |
| 新 media-agent | **80** | 新部署，包含小说+视频全部功能 |

### 14.4 端口 80 部署要求

- Uvicorn 直接监听 80 端口（或通过 Nginx 反代）
- CORS 配置更新为新端口
- `public_base_url` 更新为 `http://104.244.90.202`
- 前端 SPA 通过同端口提供服务

---

## 文档变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|---------|--------|
| 1.0 | 2026-03-29 | 初稿 | Media Agent Assistant |
| 1.1 | 2026-04-01 | 增加图片提示词评审、图片评审流程；增加部署迁移需求；更新状态机 | Media Agent Assistant |
